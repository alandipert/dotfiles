caption always '%{= kw}%?%-Lw%?%{W}[%{W}%n*%f%t%?(%u)%?%{W}]%{w}%?%+Lw%?%?%= %{g}%{W} %H %M %d %c %{g}'
activity "%c activity -> %n%f %t"
startup_message 'off'
shell /opt/local/bin/zsh
defscrollback 10000

bind R screen -t '# |root:' sudo -H /opt/local/bin/zsh 
bind a focus

escape ^Bb

screen 
screen 
screen 
screen 

select 0
